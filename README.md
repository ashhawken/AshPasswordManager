Ash Password Manager (ICTPRG302)

Run:
  python PasswordManager.py

Menu:
  [1] Add  [2] View  [q] Quit

Notes:
  - Creates/uses credentials.txt (pipe-separated: site|username|password|url)
  - Appends entries and shows an aligned table
  - No encryption (do not store real passwords)
